export default class ResourceParseSpritesheet {

    guard( contentStructList ) {
        
        // No Array
        if ( !contentStructList || !Array.isArray( contentStructList ) ) return false;

        // if ()
    }

}